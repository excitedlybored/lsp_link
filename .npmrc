# LSP-Link clone-local installation. Missing vendored dependencies must fail
# instead of falling back to a configured registry such as Artifactory.
offline=true
audit=false
fund=false
save-exact=true
