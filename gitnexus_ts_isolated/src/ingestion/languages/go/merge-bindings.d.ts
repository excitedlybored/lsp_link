import type { BindingRef } from '../../../../_shared/index.js';
export declare function goMergeBindings(existing: readonly BindingRef[], incoming: readonly BindingRef[], _scopeId: string): BindingRef[];
